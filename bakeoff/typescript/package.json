{
  "name": "trebuchet-simulator-typescript",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.ts --bundle --format=iife --platform=browser --target=es2020 --outfile=dist/bundle.js",
    "start": "node serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
